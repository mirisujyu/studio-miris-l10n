# 작업 원칙 — 누가 무엇을 결정하고, 무엇으로 확인하는가

Studio Miris 엔진(AXRe + VNEG) 게임을 한글 패치할 때의 **일하는 방식**. 순서가 아니라
판단 기준이다. 사람과 AI 에이전트가 같이 작업하는 것을 전제로 쓰였다.

| 알고 싶은 것 | 문서 |
|---|---|
| 무엇을 어떤 순서로 치는가 | [docs/BOOTSTRAP.md](docs/BOOTSTRAP.md) |
| 명령이 뭐가 있는가 | [docs/COMMANDS.md](docs/COMMANDS.md) |
| 엔진이 어떻게 생겼고 값을 어떻게 찾는가 | [SKILL.md](SKILL.md) |
| 포맷 스펙 | [docs/formats/](docs/formats/) |
| **어떻게 일하는가** | 이 문서 |

**적용 대상 확인**: 아카이브 첫 4바이트 `AXRe`, 시나리오(`.scn`) 첫 4바이트 `VNEG`.
`ujyu axr list <아카이브>` 로 한 번에 확인된다. 검증 타이틀은 神無ノ鳥(2002)이고,
다른 타이틀에서 포맷이 어긋나면 [docs/formats/](docs/formats/) 스펙과 각 문서 말미의
exe 리버싱 진입점으로 재검증한다.

---

## 1. 결정 주체 — 문답 / 분석 / 자동

작업 값은 성격이 세 갈래다. **이걸 섞으면 사고가 난다** — 취향을 에이전트가 임의로
정하거나, 리버싱으로 찾아야 할 값을 사람에게 묻거나.

### 문답으로 정한다 (값이 갈리는 선택 옵션)

에이전트는 **항목마다 트레이드오프를 제시하고 사용자 결정을 받은 뒤** 진행한다.
기본값으로 조용히 밀고 나가지 않는다. 결정과 근거가 남아 추적이 쉽다.

| 항목 | 갈리는 지점 |
|---|---|
| `FONT_WIDTH_MODE` | `fullwidth`(전각 고정폭, 문장부호도 전각 그대로) / `proportional`(가변폭, 괄호·기호 조정 + 마침표·쉼표 뒤 공백) |
| `SCALE` | 1(원본) / 2 / 3 — 화면이 커지는 정도와 업스케일 작업량의 교환 |
| 표기 정책 | 인명·용어 음차 방식, 후리가나 처리, 존칭·말투 |
| 폰트 스펙 (`fonts/<face>.py`) | 원본 폰트, 괄호 advance, 세로 시프트 기준 |
| `MOVIE_NATIVE` | 무비를 다시 인코딩해 네이티브로 재생할지 (용량 대 화질) |
| **이미지 번역 여부·범위** | 그림에 구워진 글자(타이틀 로고·UI·CG 대사)를 **번역할지**, 한다면 어디까지. 안 하면 원문 그대로 나온다 |
| **이미지 업스케일 소스** | `SCALE>1` 일 때 확대본을 **외부 도구(Topaz 등)로 준비할지**, 아니면 bilinear 폴백으로 둘지 (선명도 대 작업량) |
| **글꼴 face 이름** | 이미 설치된 동명 폰트가 있으면 GDI 가 그쪽을 고른다. 새로 만들 때 이름을 바꿀지 |

> **번역은 시작 자체를 묻는다.** 선행 조건(폰트·사전·이미지 방침·표기 규칙)이 다
> 갖춰졌는지 확인하고, 규모(`ujyu filter stats` 의 조각 수·글자 수)와 범위를 제시한
> 뒤 **시작해도 되는지 묻는다.** 냅다 돌리지 않는다 — 가장 크고 오래 걸리는 구간이고,
> 한번 굴러가면 스타일이 수천 조각에 굳는다. 절차는 BOOTSTRAP §4-3.

> **이미지는 특히 잊기 쉽다.** 텍스트만 번역해 놓고 타이틀 화면·버튼·CG 안의 글자가
> 일본어로 남아 있는 채 끝나는 일이 잦다. 착수 시점에 **반드시 물어서** 범위를 정하고,
> 안 하기로 했으면 그 결정도 남긴다. 준비물이 무엇인지는 BOOTSTRAP §4-2.

**폰트 빌드 스펙은 `config.py` 와 분리해 `fonts/<face>.py` 에 둔다**(SKILL §16).
config 는 "어느 폰트를 쓰는가", 스펙은 "그 폰트를 어떻게 가공하는가"다.

### 에이전트가 찾는다 (분석 — 문답 대상이 아니다)

exe 오프셋·주입 주소·심볼 인덱스·씬 내 치수 오프셋처럼 **리버싱이나 데이터 대조로
확정되는 값**은 정답이 하나다. 사용자에게 물을 것이 아니라 찾아서 채운다.

```bash
ujyu exe scan <원본exe> --config      # 대부분 여기서 나온다
ujyu exe disasm <exe> at|fn|xref|imm   # 스캐너가 못 찾는 것만
```

찾은 값은 반드시 **데이터로 검증**한다. 예: exe 오프셋을 채운 뒤 원본에서 다시 빌드해
동작하던 배포본과 바이트가 같은지 대조한다. 값을 넣고 "될 것"이라며 넘어가지 않는다.

### 명령이 산출한다 (자동)

`ORIG_W`/`ORIG_H`(common.csv 에서 읽음), 화자 집계, 아카이브 종류 판별 같은 것.
사람이 옮겨 적지 않는다.

### 상태는 `inspect` 로 본다

```bash
ujyu inspect            # 무엇이 비었고, 그걸 채우는 명령이 무엇인지
ujyu config set KEY V   # 값 유효성을 검사한 뒤 config.py 의 그 줄만 고친다
```

`inspect` 는 문제가 있으면 종료 코드 1 이라 스크립트에서도 쓸 수 있다.

## 2. 작업 원칙

1. **한 번에 하나.** 작업 하나를 끝내고 다음으로 간다. 여러 접근을 동시에 벌이지 않는다.
2. **격리 테스트.** 한 번에 변수 하나만 바꾼다. 결과를 해석하기 전에 *"의도한 것 말고
   딴 걸 오염시키지 않았나"* 를 되짚는다.
3. **가정을 데이터로 검증.** 절대 오프셋인지 제어코드인지 같은 판단은 **여러 씬을 대조**해
   확인한다(상수면 제어코드, 씬마다 다르면 오프셋).
4. **원본은 읽기 전용.** `ORIG_DIR` 에는 절대 쓰지 않는다. 빌드는 매번 원본에서 다시
   생성해 `GAME_DIR` 에 쓴다 — 그래야 몇 번을 돌려도 결과가 같다.
5. **수동 편집을 남기지 않는다.** 손으로 고쳐서 동작을 맞췄다면 그 값을 **config 로
   끌어올려 빌드가 재현**하게 한다. 안 그러면 다음 빌드가 조용히 되돌린다.
6. **알아낸 구조는 되돌려 준다.** 새 opcode·심볼·구조를 파악하면 그때그때
   `ujyu/formats/` 와 [docs/formats/](docs/formats/) 스펙에 반영한다. 이 리포의 유지 원칙이다.
7. **사실대로 보고.** 실패·미확인은 그대로 알린다. 성과를 부풀리지 않는다.

## 3. 검증

| 무엇 | 어떻게 |
|---|---|
| config 상태 | `ujyu inspect` (문제 0 확인) |
| 번역 제약 | `ujyu inject check` — CP949 불가 문자·제어코드 불일치 전수 검사. **0건이 되기 전에는 빌드하지 않는다** |
| 아카이브 구조 | `ujyu scn disasm <배포본> -o _verify` — KR 텍스트·정의/런타임 심볼 수·점프테이블 |
| common.csv | `ujyu csv show <배포본>` — 제목·해상도 반영 |
| 이미지 명세 | `ujyu image --check` (렌더 없이 명세·입력만) |
| exe 재현성 | 원본에서 다시 빌드해 배포본과 바이트 대조 |
| 실게임 | 오프닝·**선택지**·무비 구간. 특정 씬은 `ujyu save goto <씬>` 으로 바로 진입 |

선택지와 무비는 점프테이블을 타므로 **반드시 눌러 본다**. 대사만 확인하면 재매핑
문제를 놓친다.

## 4. 증상별 진단

| 증상 | 원인 | 문서 |
|---|---|---|
| `…の読み出しに失敗` | 리팩 시 헤더 +8 quirk | [AXR.md](docs/formats/AXR.md) |
| 무비·선택지에서 멈춤 | 점프테이블 재매핑 누락 (flow 상대 오프셋이 스테일) | [VNEG.md](docs/formats/VNEG.md) §3 |
| 특정 화면에서만 크래시 | `.scn` 명시 치수를 ×N 안 했다 (clamp) | [RESOLUTION.md](docs/formats/RESOLUTION.md) §15-4·5 |
| 선택지 진입 시 크래시 | common.csv 를 **다른 아카이브 것으로 통째 주입**해 고유 변수 정의가 사라졌다 | [COMMON_CSV.md](docs/formats/COMMON_CSV.md) |
| 글자 깨짐 / 한 글자씩 쪼개짐 | 리드바이트 비트맵 미패치 | [TEXT_RENDER.md](docs/formats/TEXT_RENDER.md) §1 |
| 공백이 밀림 / ASCII 가 사라짐 | 전각 규칙 위반 (표시 바이트는 ≥ 0x80) | SKILL §2·6 |
| 글꼴 목록에 폰트가 안 뜸 | 고정폭·비비례·TrueType 필터 | SKILL §8 |
| 선택지 창이 좌상단에 몰림 | 스케일 비대상 창 — `ujyu scale center` (config `COMMON_CENTER`) | [RESOLUTION.md](docs/formats/RESOLUTION.md) |

## 5. 실무 함정

실제로 데어 본 것들. 처음 겪으면 원인을 찾기 어렵다.

- **콘솔은 CP949 다.** em 대시(`—`)·장음(`ー`) 같은 문자를 print 하면 그 자리에서 죽는다.
  일본어 원문을 확인해야 하면 **UTF-8 파일로 쓰고 읽는다**.
- **번역문의 늘임표.** `—`(U+2014)·`ー` 는 CP949 에 없다. 주입기가 `―`(U+2015)로
  정규화하지만, 새 문자를 쓸 때는 CP949 인코딩 여부를 먼저 확인한다.
- **어절 구분은 전각 공백**(U+3000). 반각 스페이스는 엔진이 커맨드로 소비한다.
- **`str.strip()` 은 전각 공백까지 지운다.** 공백만으로 된 값(익명 화자 네임플레이트
  등)이 조용히 사라진다.
- **JP(SJIS)와 KR(CP949) 바이트를 difflib 로 섞지 말 것.** 우연 정렬로 2바이트 문자
  중간에 착지한다. 구조 정보(치환 스팬)가 있으면 그걸 쓰는 게 정확하고 100배 빠르다.
- **common.csv 는 아카이브마다 자기 것을 제자리 편집.** 아카이브별로 고유 변수 정의가
  있어서 다른 것을 주입하면 미초기화 크래시가 난다.
- **폰트를 다시 설치해도 안 바뀌면** GDI 가 같은 face/파일명의 글리프 테이블을 캐시한
  것이다. 새 파일명으로 설치한다.
- **세이브의 씬 이름은 같은 길이여야** 바꿀 수 있다(`ujyu save goto` 가 검사해 준다).
- **배포 패키지에 세이브·작업 백업이 섞이지 않게** `RELEASE_EXCLUDE` 를 채운다.
  기본값 없이 돌리면 사용자 세이브를 덮어쓰는 패치가 나온다.
- **Topaz Photo AI 의 CLI(`tpai.exe`)는 엔터프라이즈 계정에서만 열린다.** 개인
  라이선스면 GUI 배치로 돌리고 결과 폴더를 `ujyu scale cg --from-dir` 로 넘긴다.

## 6. 사람이 해야 하는 일

명령으로 닫을 수 없는 것 — 자동화 대상이 아니다.

- **번역 그 자체** (말투·표기·문맥 판단)
- **인물·용어 사전** — `CHARACTERS.md`·`GLOSSARY.md`. 외부 출처 조사 + 표기 결정.
  **번역 착수 전에** 만들어야 스타일이 흔들리지 않는다.
- **무문자 베이스 이미지** — 글자를 지우고 배경을 복원하는 그림 작업
- **이미지 manifest 의 bbox·색** — 원본을 보고 재는 일
- **스캐너가 못 찾는 exe 오프셋** — 콜백 구조·의미 판단

---

명령이 실패하거나 포맷이 안 맞으면: [docs/formats/](docs/formats/) 스펙 → SKILL.md 의
"찾는 법" → 그래도 다르면 그 타이틀의 exe 를 디버거로 재검증하고, 알아낸 구조를
`ujyu/formats/` 와 스펙 문서에 반영해 되돌려 준다.
