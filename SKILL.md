---
name: studio-miris-engine
description: >
  Studio Miris (すたじおみりす) VN 엔진 — AXRe 아카이브 + VNEG 스크립트 — 의
  한글 패치 가이드. 아카이브 언팩/리팩, 엔진의 2바이트 판정 구조,
  CP949 직접 렌더 패치, 텍스트 인코딩 규칙, 폰트 가공, 글꼴 UI 패치, 코드 케이브,
  이미지 텍스트 지역화. 이 엔진 기반 타이틀을 한글 패치하거나 리버싱할 때 읽는다.
  검증 타이틀: 神無ノ鳥 (2002).
---

# Studio Miris 엔진 한글 패치 가이드

**대상 엔진**: AXRe 아카이브 + VNEG 스크립트. 32-bit x86 PE, SJIS(CP932) 전제.
타이틀에 따라 주소는 다르므로 **각 항목마다 "찾는 법"을 먼저** 쓰고, 검증된 실측값은
부록에 둔다.

> **엔진 식별**: 아카이브 파일 첫 4바이트가 **`AXRe`**, 스크립트(`.scn`) 첫 4바이트가 **`VNEG`**이면 이 가이드에 따라 한글 패치가 가능할 것으로 예상된다.

---

## 1. 아카이브 (AXRe)

xorshift 스트림 암호. 헤더 16바이트 + 암호화된 인덱스 + 파일 데이터.

```python
from miris import axr as A
data, entries, tbl = A.load("scenario.axr")   # entries = [(name, off, size)]
buf = A.getfile(data, tbl, off, size)
blk2 = int.from_bytes(open("scenario.axr","rb").read()[8:12], "little")
A.pack([(name, bytes), ...], blk2)            # 원본 순서 유지
```

- 인덱스 크기 필드는 **실제 인덱스 바이트 + 8** (엔진 quirk). 리팩 시 이 규칙을 지켜야
  콘텐츠 키스트림 시드가 원본과 맞는다. 상세는 `docs/formats/AXR.md`.
- **패치 아카이브 구조**: `scenario.axr` 외에 `scenario.ax2`/`ax3`/`ax4` 가 있으면
  이들이 같은 이름 파일을 **덮어쓴다** (번호 큰 쪽 우선). 원본 아카이브에만 주입하면
  일부 파일이 조용히 무효화된다. 개정판이라 오프셋이 달라 **원문 텍스트로 매칭**해 주입한다.
- 설정은 아카이브 안 **`common.csv`** 에 있다 (`file,...` 로 세이브 경로, `string,title,...` 로 창 제목).

---

## 2. 텍스트 모델

`.scn` 안 대사는 **평문 SJIS**다. 리더가 바이트 값으로 분기한다:

| 바이트 | 처리 |
|---|---|
| `0x00`–`0x2F` | 제어코드 점프테이블 |
| `0x30`–`0x7F` | 커맨드 (일부는 뒤 2바이트를 오퍼랜드로 삼킴) |
| `0x80`–`0xFF` | 표시 문자 — 2바이트 조립 |

### 리더 찾는 법
`cmp reg, 0x2F` → `ja` 로 시작해 `0x50`/`0x60`/`0x70`/`0x80` 을 차례로 비교하는 사다리.
`0x80` 분기 끝에 `shl reg,8; or reg,<trail>` 이 있으면 그게 2바이트 조립부다.

> **이 바이트 모델은 왜 여전히 필요한가.** 위 exe 리버싱(리더 사다리·2바이트 조립)은 **새
> 타이틀에서 모델을 확인하고** CP949 리드바이트 비트맵 위치(6절)를 찾는 근간이다 — 디스어셈블러가
> 대체하지 못한다. 반대로 **알려진 타이틀의 스크립트에서 "어디가 텍스트/스트링인지"는 손으로
> 바이트를 분류하지 말고** VNEG 디스어셈블러(`ujyu` = `ujyu.formats.vneg`)에 맡긴다. 그것이 이
> 바이트 모델을 구현해 심볼(전체 23타입)·점프테이블·대사 런의 오프셋(스트링 포인트)을 정확히
> 찍는다. 즉 모델의 스펙은 [`docs/formats/VNEG.md`](docs/formats/VNEG.md) 한 곳, 적용은 도구가 한다.

### 번역문 제약
본문 메시지 경로에서는 표시되는 모든 바이트가 `0x80` 이상이어야 한다.
거기선 ASCII 가 전부 커맨드다 (실측: ASCII 공백에서 텍스트가 끊기고, ASCII 영숫자는
아예 표시되지 않는다). 그래서 번역문의 ASCII 는 전각으로 정규화한다.

추출된 "텍스트 런"에는 커맨드 바이트가 섞여 있다. 다음 순서로 처리한다:

1. 원문 런에서 커맨드를 식별해 `{n}` 토큰으로 치환한다.
   - 꼬리: 대사 끝 `」%` 의 `%`(`0x25`) = 대기 커맨드 → `」{0}`
   - 시퀀스: `\n`(`5C 6E`) = 줄바꿈 커맨드 → `{0}`
2. 번역자는 토큰을 그대로 둔 채 표시 텍스트만 고친다.
3. 주입할 때 토큰을 원본 커맨드 바이트로 복원한다.

```
원문   ん？　なんだ？」%        →  템플릿  ん？　なんだ？」{0}
번역   뭐야?　무슨 일이야?」{0}  →  주입    CP949 표시문자 … + 25
```

토큰은 런 안 어디에 있든 위치와 무관하게 복원된다.
런 전체를 전각 정규화하면 커맨드가 표시 문자로 바뀌어 스크립트가 깨진다
(`%` → `％`, `\n` → `＼ｎ`).

> 어떤 바이트가 커맨드인지는 텍스트 경로마다 다르다. 본문 메시지 경로는 `0x00`–`0x7F`
> 전부를 커맨드로 소비하지만, 스태프롤·크레딧 등은 ASCII 를 그대로 표시한다
> (원문에 ` teamL` `-…-` 같은 ASCII 가 실제로 들어 있다).
> 도구는 확실한 것만 토큰화한다 — 꼬리의 연속 제어코드(`0x30` 미만)와
> `CMD_SEQS` 에 등록된 시퀀스. 나머지 ASCII 는 표시 텍스트로 보고 전각 정규화하는데,
> 본문에서도 다른 화면에서도 그대로 표시된다.
> 타이틀마다 커맨드를 더 발견하면 `CMD_SEQS` 에 추가한다.

분리가 끝난 표시 텍스트에만 정규화를 적용한다:

- ASCII 공백·영숫자·기호 → 전각. 전각공백 `U+3000`=`A1A1`, 전각ASCII=`A3xx`.
- CP949 에 없는 문자는 있는 것으로 치환 (`・`U+30FB → `·`U+00B7 등).
- 일본어 마침표/쉼표 `。`/`、`(U+3002/U+3001) → 전각 `．`/`，`(U+FF0E/U+FF0C). `A3xx`
  범위라 폰트 비례 매핑(7절)으로 좁은 `.`/`,` 처럼 보인다. 뒤에 **기타 문자(문자·숫자)**
  가 이어지면 반각 폭 공백(전각공백 `U+3000`; 이 폰트가 반각으로 렌더) 한 칸을 넣고,
  뒤가 **기호이거나 문장 끝**이면 넣지 않는다 (`はい。」`→`はい．」`, `そう。ん`→`そう．　ん`).
  미번역분을 SJIS→CP949 변환할 때도 같은 규칙을 적용해야 화면 표기가 통일된다.

---

## 3. VNEG 스크립트 구조

### 제어코드 디스패치

`0x00`–`0x2F` 는 48엔트리 점프테이블로 간다 (`jmp [reg*4 + <TABLE>]`).
`0x30`–`0x7F` 는 범위별 핸들러 4개(`0x30-4F`/`0x50-5F`/`0x60-6F`/`0x70-7F`)로 가고,
대개 `커맨드 인덱스 = byte - 범위시작` 으로 vtable 간접호출한다.

오퍼랜드는 빅엔디안 16비트다. 핸들러가 이렇게 읽는다:

```asm
mov dl, [ptr+1]      ; 하위 바이트
mov dh, [ptr]        ; 상위 바이트   → dx = 빅엔디안 워드
add ecx, 2           ; 스크립트 포인터 전진
mov [esi+0x48], ecx  ; 커밋 (0x48 = 스크립트 포인터)
```

주요 코드 (神無ノ鳥 실측 빈도, 506개 `.scn` 기준):

| 코드 | 빈도 | 역할 |
|---|---|---|
| `0x00` | 717,205 | 상위 바이트 / 종료 (`[esi+0x24]=1` 후 리턴) |
| `0x01` | 500,953 | 워드 오퍼랜드 커맨드. `01 <hi> <lo>` |
| `0x10` | 299,844 | `0x01` 의 오퍼랜드 상위 바이트로 자주 등장 |
| `0x02` | 213,386 | `0x01` 과 같은 핸들러, 추가 디스패치 |
| `0x0D` | 114,379 | 대사 프레임 구성 |
| `0x04` | 100,895 | 블록 종료 — `[esi+0x4c]=[esi+0x48]` 후 리턴 |
| `0x25` (`%`) | 27,891 | 대기 / 클릭 대기 — 대사 끝 |
| `0x2A` (`*`) | 14,894 | 커맨드 (자주 `*v` 형태) |

### 심볼 테이블

`.scn` 앞부분에 문자열·정수 상수가 나열된다. 화자명·리소스명이 여기 있다.
일반 문법은 `<op:u8> <slots:u16 BE> <argc:u8> <args>`이며 아래는
단일 슬롯 정의의 예다.

```
문자열:  03 00 01 01 00  <널 종단 문자열>
정수:    02 00 01 01 01  <4바이트>
```

런타임 인덱스는 정의의 등장 순서가 아니라 앞선 정의들의 `slots` 합이다.
`slots > 1`인 배열 정의 뒤에서는 둘이 달라진다.

### 대사 / 나레이션 프레임

```
0D 33 01 <symref:u16> ; ← 화자 = 런타임 심볼테이블[idx] (네임플레이트)
01 00 0D
<xx> 00
81 75  또는  81 69  ; 「(대사) / （(마음속 독백) — 이건 표시 문자다
7A                 ; 'z' 커맨드
01 10 02           ; (제어 바이트들)
<대사 텍스트>       ; SJIS/CP949 2바이트 문자열
25                 ; '%' 대기
00 16 04 7F        ; 블록 종료
```

나레이션은 여는 괄호 없이 모드값 0인 심볼참조 뒤에 텍스트가 온다.
`00 0E 00 01 10 04`는 그 한 예다.

⚠️ 여기서 `10 02`/`10 04`는 고정 상수가 아니라 **심볼참조 워드**다.
그 심볼의 정수 값이 표시 모드다: **int 0 = 나레이션, int 1 = 대사**. 모드 심볼의
인덱스는 씬마다 다르다 (03/05/06/07/08/09… 실측). 인코딩은
`0x1000 + idx`이므로 `11 00`은 심볼 #256이다. 텍스트 블록 판별 전체 규칙(인라인 커맨드
`04 XX 00` 클릭대기, `01 10 XX` 보이스/BGM 삽입, `0x80` 글리치 등)은
`docs/formats/VNEG.md` §4-2 와 `ujyu.formats.vneg.extract` 구현 참조.

실측 (텍스트 직전 6바이트 상위 패턴):

| 패턴 | 횟수 | 의미 |
|---|---|---|
| `81 75 7A 01 10 02` | 25,947 | 대사 (`「z`) |
| `00 0E 00 01 10 04` | 7,595 | 나레이션 |
| `81 69 7A 01 10 02` | 6,629 | 마음속 독백 (`（z`) |
| `00 03 00 01 01 00` | 1,407 | 심볼 테이블 문자열 |

텍스트 직후: `00 16 04 7F` (26,338) → `04 7F 00 74` (18,160).

---

## 4. 화자(네임플레이트) 자동 마킹

대사마다 화자를 붙여두면 상대별 말투를 일관되게 번역할 수 있다. **정답 소스는
SPEAKER opcode `0d 33 01 10 <sym>`** — 네임윈도우에 표시할 이름 심볼을 지정한다.
`ujyu.formats.vneg.extract` 가 이 방식으로 dlg/quote 레코드에 `speaker` 를 붙여 준다.

> 과거에는 보이스 프레임(`01 10 <idx>` + 「/（ + z)에서 추정했는데, **보이스 없는
> 대사(………)에서 직전 화자를 잘못 물려받는다** (神無ノ鳥 실측 365건 오류).
> 아래 코드는 그 구식 방법의 기록이다 — 새 타이틀에서는 extract 를 쓸 것.

```python
import re
# 심볼 테이블에 섞인 리소스명 — 화자로 잡히지 않게 거른다 (타이틀마다 조정)
RESOURCE_RE = re.compile(r'^(f_|k6\d|bg\d|se_|m_a|event\d|movie/|.*_se$|se$)')

def parse_syms(d):
    """심볼 테이블 → [문자열 또는 None(정수)] (등장 순서)"""
    syms=[]; i=0
    while i < len(d)-5:
        if d[i+1:i+4] == b'\x00\x01\x01':
            if d[i] == 0x03:                        # 문자열
                e = d.find(b'\x00', i+5)
                if e < 0: break
                syms.append(d[i+5:e].decode('cp932','replace')); i = e+1; continue
            if d[i] == 0x02:                        # 정수
                syms.append(None); i += 9; continue
        i += 1
    return syms

def tag_scene(d):
    """{텍스트오프셋: 화자명}"""
    syms = parse_syms(d); out = {}; i = 0
    while i < len(d)-11:
        if (d[i:i+2] == b'\x01\x10' and d[i+3:i+6] == b'\x01\x00\x0d'
                and d[i+7] == 0x00
                and d[i+8:i+10] in (b'\x81\x75', b'\x81\x69')   # 「 또는 （
                and d[i+10] == 0x7a):                            # z
            spk = syms[d[i+2]] if d[i+2] < len(syms) else None
            j = i+11
            while j < len(d) and d[j] < 0x20: j += 1              # 제어바이트 건너뛰기
            if spk and not RESOURCE_RE.match(spk): out[j] = spk
            i = j
        else:
            i += 1
    return out
```

심볼 테이블에는 리소스명(`bg00a`, `se_wind_lp1`, `m_a_01`, `f_…`)도 섞여 있다.
정규식으로 거르지 않으면 배경 파일명이 화자로 붙는다.

神無ノ鳥 실측: 대사 33,601건에 화자 태깅, 고유 네임플레이트 53개.
결과는 `strings.json` 의 `speaker` 필드로 들어가고, 번역 덤프에 같이 실어 말투를 맞춘다.

---

## 5. 제어코드가 SJIS 텍스트처럼 보이는 이유

> **현행 파이프라인은 이 문제를 겪지 않는다** — `ujyu scn extract`(=`ujyu.formats.vneg.extract`)는
> VNEG 를 디코드해 텍스트 블록 안의 런만 뽑으므로 오퍼랜드 오탐이 없다 (VNEG.md §4-2).
> 이 절은 왜 "디코드 가능한 런" 휴리스틱이 실패하는지의 기록이자, extract 결과를
> 검증할 때(예: `ujyu migrate` 커버리지 QA)의 배경 지식이다.

텍스트 추출기는 보통 "SJIS 로 디코드 가능한 바이트 런"을 텍스트로 잡는다:
2바이트 SJIS 쌍 + 반각가나(`0xA1`–`0xDF`) + ASCII(`0x20`–`0x7E`).

커맨드와 오퍼랜드의 바이트 값이 이 범위에 자주 들어가므로, 바이트만 봐서는
텍스트와 구분되지 않는다.

| 화면에 보이는 것 | 실제 정체 |
|---|---|
| `z` (0x7A) | 대사 프레임의 커맨드 opcode (인쇄 가능 ASCII 라 텍스트로 보임) |
| `%` (0x25), `*` (0x2A) | 대기/커맨드 opcode |
| `ｱ ｲ ｳ ﾜ ﾟ` 등 반각가나 | 오퍼랜드 바이트 (`0xA1`–`0xDF`). 리소스 ID·좌표 등 |
| `钁` `鏃` `躪` `纊` `喙` 같은 희귀 한자 | 2바이트 오퍼랜드가 우연히 유효 SJIS 쌍이 된 것. 예: `钁` = `E8 76` (`0xE8`=SJIS 리드, `0x76`=`'v'` 트레일) |
| `「z` `（z` | 표시 문자 `「` + 커맨드 `z` 가 한 런으로 붙어 잡힘 |

### 선별 규칙

- **번역 대상**: 히라가나/가타카나/한자 등 표시용 2바이트 문자를 최소 1자 포함한 런.
- **제외**: 전부 반각가나거나 전부 ASCII 인 런 = 오퍼랜드/커맨드.
- **제외**: 리소스명 패턴 (`f_…` `bg\d` `se_…` `m_a…` `event\d` `movie/…`).
- **제외**: 특정 희귀 한자 1자짜리 마커 (해당 타이틀에서 빈도·문맥으로 식별. 神無ノ鳥 는 `钁` 331회).
- **꼬리 제어코드 분리**: 대사 런은 대개 `」%` 로 끝난다. `%`(0x25) 같은 `<0x30` 꼬리는
  번역 전에 떼어내고, 주입할 때 원본 바이트 그대로 다시 붙인다.

이 규칙이 번역 대상을 실질적으로 절반으로 줄인다 (神無ノ鳥: 휴리스틱 89,016조각 →
구조적 추출 80,949조각, 그중 번역 대상은 대사·나레이션·심볼 문자열).

---

## 6. CP949 직접 렌더

> 구조 스펙(무엇이 왜 그런가)은 [docs/formats/TEXT_RENDER.md](docs/formats/TEXT_RENDER.md)
> — 이 절부터 10절까지는 **타이틀별 주소를 찾는 법**이다.

> 일부 엔진은 Shift JIS 코드를 강제하기 때문에 글꼴의 슬롯 치환이 필요한 경우가 있지만, 이 엔진의 경우 간단한 바이트 코드 조작으로 CP949를 표시할 수 있는 것으로 확인되었다.

### 6-1. 리드바이트 비트맵

문자 추출 함수가 256비트(32바이트) 비트맵으로 2바이트 리드 여부를 판정한다.

```asm
mov  edx, 0x80
and  ecx, 7
sar  edx, cl              ; 마스크 = 0x80 >> (byte & 7)
shr  ecx, 3               ; byte / 8
mov  cl, [ecx + <TABLE>]  ; ← 비트맵
test cl, dl
je   <1바이트 처리>
```

**찾는 법**: 위 idiom 을 디스어셈블에서 검색한다. `sar reg,cl` + `shr reg,3` +
`mov cl,[reg+imm32]` + `test` 조합이 특징. `<TABLE>` 이 비트맵 주소다.

판정 테이블은 **32바이트 비트맵**(1비트 = 1바이트값)이다. 이 형태가 안 보이면
256바이트 룩업 테이블이나 명시적 범위 비교(`cmp`) 방식도 확인한다.

**패치**: 비트맵을 CP949 리드 범위 `0x81`–`0xFE` 로 재생성한다.

```python
new = bytearray(32)
for i in range(0x81, 0xFF):
    new[i >> 3] |= (0x80 >> (i & 7))
# → 00×16 7F FF FF FF FF FF FF FF FF FF FF FF FF FF FF FE
```

SJIS 리드(`0x81`–`0x9F`, `0xE0`–`0xFC`)도 이 범위에 포함되므로 원문 일본어도 그대로 동작한다.

같은 함수에 **모드 인자**가 있어, 모드에 따라 비트맵 대신 `cmp eax,0x80; jb`
(0x80 이상이면 무조건 2바이트)를 쓰는 분기가 있을 수 있다. 그쪽은 원래부터 CP949 호환이다.

### 6-2. 폰트 charset

`mov byte [esp+XX], 0x80` (= `C6 44 24 ?? 80`) 패턴을 **전수 검색**한다.
보통 2곳이다 — 본문 폰트용, 글꼴 열거용. 둘 다 `0x81`(HANGUL)로 바꾼다.

> 열거용을 바꾸지 않으면 글꼴 목록에 한글 폰트가 나오지 않는다.

### 6-3. SJIS 리드 판정 idiom

```asm
mov edx, eax
xor edx, 0x20
sub edx, 0xA1
cmp edx, 0x3C     ; (lead ^ 0x20) ∈ [0xA1,0xDC] == SJIS 리드
jae <1바이트>
```

메뉴·문자열 검증 등 부수 경로에 몇 곳 있다. `sub edx,0x81; cmp edx,0x7E` 로 바꾸면
`0x81`–`0xFE` 를 인정한다.

`sub` 의 즉치는 imm32 (`81 EA 81 00 00 00`) 로 쓴다. imm8(`83 EA 81`) 은 `0x81` 이
부호확장되어 덧셈이 된다.

---

## 7. 폰트

렌더는 보통 `GetGlyphOutlineA(GGO_GRAY8)` 경로다. 자간은 폰트 글리프의 advance 가 결정한다:

```asm
call <draw_char>   ; 글자 그리고 폭을 EAX 로 반환
add  edi, eax      ; ← x 좌표 전진
```

폭·비례·자간은 exe 가 아니라 폰트를 고쳐서 조절한다.

### 폰트 가공 4단계 (`ujyu font`)

1. **세로 시프트** — GGO 경로는 글리프 아웃라인 좌표와 MAT2 행렬만 사용한다.
   하단 잘림은 아웃라인 자체를 위로 올려서 고친다 (OS/2·hhea 메트릭 수정은 이 경로에 영향 없음).
   기준: 한글 최저점이 `+0.031 em` 에 오도록. 표본 음절 최저점을 재서 자동 계산.
2. **전각 공백 폭** = 한글 폭의 절반. CP949 엔 `0x80` 이상인 좁은 공백이 없어
   낱말 사이가 전부 전각 공백이 되기 때문.
3. **전각 ASCII** `U+FF01`–`FF5E` → 비례 ASCII 글리프 재매핑.
   바이트는 `A3xx` 그대로라 파싱엔 영향 없고 화면만 자연스러워진다.
4. **CFF(OpenType) → glyf(TrueType) 변환** — 엔진이 OpenType 을 목록에서 걸러낸다.
   `Cu2QuPen` 으로 3차→2차 베지어, `CFF `/`VORG`/`DSIG` 제거, `maxp` 1.0 + long `loca`.
   `sfntVersion` 은 `00010000` 으로 바꾼다. `OTTO` 로 남으면 내용은 glyf 인데 헤더는
   CFF 라 선언하는 꼴이 되어 Windows 가 설치를 거부한다.

---

## 8. 글꼴 선택 UI

### 8-1. 목록 필터

`EnumFontFamiliesExA` 콜백이 후보를 거른다. 원작은 고정폭·비비례·TrueType 만 통과시켜,
한글 가변폭 폰트(맑은고딕 등)가 전부 탈락한다.

```asm
test al, 4                    ; TRUETYPE_FONTTYPE 만        → OpenType 탈락
call <strstr>; test; jne skip ; "@"(세로쓰기) "$" "P" "Ｐ"(비례) 이름 제외
mov  dl,[lpelfe+0x1B]         ; lfPitchAndFamily
and  dl,1; cmp dl,1; jne skip ; FIXED_PITCH 강제            ← 가변폭이 여기서 탈락한다
mov  al,[lpelfe+0x18]         ; lfOutPrecision
and  al,3; cmp al,3; jne skip ; 아웃라인
```

- 가변폭 허용: FIXED_PITCH 검사와 `P`/`Ｐ` 이름 제외의 `jne` 를 **NOP**.
- **이름 필터로 전환**: 제외 필터 하나를 골라 `jne`→`je` 로 뒤집고, 패턴 문자열을
  원하는 접두어(예: `Kannagi\0`)로 교체하면 그 접두어 폰트만 목록에 뜬다.
  패턴 문자열은 기존 `P`/`Ｐ` 자리(널 패딩 포함 8바이트)를 재활용할 수 있다.

### 8-2. 기본 글꼴

설정 변수에 exe 내 SJIS 문자열을 기본값으로 등록하는 코드가 있다:

```asm
push <GOTHIC_STR>      ; "ＭＳ ゴシック"
push 0
mov  ecx, [obj+0xF8]
call [vt+8]
```

**찾는 법**: `ＭＳ ゴシック` / `ＭＳ 明朝` SJIS 바이트를 exe 에서 검색 →
그 주소를 `push` 하는 코드를 역참조. 두 문자열은 `.data`(쓰기 가능) 이고
보통 널 패딩 포함 16바이트 슬롯이다.

한국어 Windows 엔 이 폰트들이 없어 GDI 가 굴림으로 대체한다. 슬롯을 우리 폰트명으로
덮으면 기본 글꼴이 잡힌다.

> 기본 글꼴은 이 exe 슬롯으로 잡는다. `common.csv` 의 `font_face`/`MINCHO`/`GOTHIC`
> 값은 검증 타이틀에서 반영되지 않았으므로, 타이틀별로 확인한 뒤 결정한다.

### 8-3. face 선택 분기

폰트 빌더 안에 `arg & 0xff` 로 갈리는 분기가 있다 (1/2 = 호출자가 준 이름, 그 외 = 하드코딩 폴백).
이 분기를 NOP 해서 폴백을 강제하면 글꼴 창의 선택이 무시된다. 기본값은 8-2 로 잡고
분기는 그대로 둔다.

---

## 9. 글꼴 저장/복원 (코드 케이브)

원작은 글꼴을 저장만 하고 재시작 시 읽지 않는다. 저장된 이름이
`CreateFontIndirectA` 까지 도달하지 않는다 (`CreateFontIndirectA` 직전에 로그 브레이크포인트를
걸어 face 이름을 찍으면 확인된다).

코드 케이브로 보완한다:

```
엔트리포인트 → 케이브 (.text 끝 패딩, 보통 수백~수천 바이트 여유)
  CreateFileA("save\systemdata.dat", GENERIC_READ, ...)   ; 없으면 통과
  ReadFile(h, <.data 여유구간>, 64, &n, 0)
  CloseHandle
  buf+0x08 의 널 종단 글꼴명을 8-2 의 기본값 슬롯에 최대 15자+널 복사
  → 원래 OEP 로 jmp
```

`systemdata.dat` 앞부분: `+0x00` 헤더 4B, `+0x04` 4B, `+0x08` 글꼴명(널 종단).

**안전장치**: 파일 없음 / 읽은 바이트 부족 / 이름 첫 글자가 `0x20` 미만이면 아무것도
건드리지 않는다. 이 경우 기존 기본값으로 동작한다.

### 케이브 자리 찾기
- 코드: `.text` 의 `VirtualSize`~`SizeOfRawData` 사이 패딩 (전부 `00`).
- 버퍼: `.data`(쓰기 가능) 안의 긴 `00` 구간.
- 두 섹션의 `Characteristics` 로 실행/쓰기 가능 여부를 확인한다.

---

## 10. Windows UI

> 구조 스펙(리소스 형식·인라인 치환 규칙·주의)은
> [docs/formats/WINDOWS_UI.md](docs/formats/WINDOWS_UI.md).

- 리소스(MENU/DIALOG/STRINGTABLE)는 `UpdateResourceW` 로 UTF-16 으로 넣는다 (코드페이지 무관).
- 인라인 ANSI 문자열(MessageBox 등)은 CP949 로 제자리 치환한다.
- 다이얼로그 폰트도 한글 폰트로 바꾼다.
- 메뉴는 numeric ID 가 아니라 문자열 이름(`MYMENU` 등)으로 참조될 수 있다.
  `UpdateResourceW` 에 `LPCWSTR` 로 넘겨야 한다.
- 번역표 양식: [`samples/ui_strings.sample.json`](samples/ui_strings.sample.json).

---

## 11. 번역 준비 — 고유명사·인물 사전 구축

> **작업 전 필수: 타이틀 리포에 사전이 이미 있으면 먼저 읽는다.**
> 대사·나레이션을 한 줄이라도 번역하기 전에, 타이틀 리포의 `translation/` 아래
> 가이드 문서를 **먼저 전부 읽고** 그 표기·말투·검수 규칙을 따른다. 이걸 건너뛰고
> 임의 스타일로 번역하면 뒤에 전체를 되돌린다. 통상 갖춰지는 문서:
> - `CHARACTERS.md` — 인물 1인칭·말투·**상대별 말투(Relationship Register)**. 화자(speaker) 필드에 맞춰 어투 통일.
> - `GLOSSARY.md` — 고유명사 표기표 + **후리가나 `【】` 확정표**(가나 읽기/한자 독음 중 택1로 확정, 병기 금지).
> - `STATUS.md` — 진행 현황(루트/날짜/파일). 파일명 순=이야기 순. **번역을 진행하면 이 문서에 진척(완료 씬·비율)을 갱신 기록한다.**
> - `REVIEW_NOTES.md` + `TRANSLATION_ALTERNATIVES.md` — 일본식 조어 한자어를 그대로 음독하는 오역 방지(§2 false friend 우선).
>
> 사전이 **없을 때만** 아래 절차로 새로 구축한다.

번역을 시작하기 전에 고유명사 표기와 인물별 말투를 확정한다.
확정 없이 진행하면 같은 이름이 파일마다 다르게 나오고, 뒤에 전체를 되돌리게 된다.

이 사전들은 살아있는 문서다. 번역하면서 새 용어·새 화자가 나올 때마다 갱신하고,
확정 전 항목은 `※` 로 표시해 검수 때 걸리게 한다.

### 11-1. 외부 출처로 사전 판단

| 출처 | 얻을 수 있는 것 |
|---|---|
| 공식 사이트 (폐쇄됐으면 **web.archive.org**) | 인물 프로필, 신장/생일/혈액형, 성격 설명, 관계도, 성우 |
| 외부 작품 DB | 인명 로마자 표기(읽기 확정), 인물 관계, 발매 정보 |
| 공식 가이드북 / 팬디스크 | 세계관 용어, 지명, 설정 용어 |
| 게임 내 **스태프롤·인물소개 화면** | 공식 표기와 성우. 아카이브에서 바로 추출 가능 |

읽기(발음)가 갈리는 인명은 공식 페이지의 파일명 로마자가 근거가 되는 경우가 많다
(`ikaru.jpg` 처럼). 외부 DB 의 로마자 표기로 교차검증한다.

### 11-2. 스크립트 파싱으로 뽑아내기

외부 자료가 없거나 부족하면 스크립트에서 직접 뽑는다.

**주연 판별 — 네임플레이트 빈도**

4절의 화자 태깅을 돌리면 화자별 대사 수가 나온다. 빈도 상위가 주연이다.
같은 인물이 성/이름 두 네임플레이트로 등장하는 경우가 흔하니(예: `深町` 3,212건 /
`康哉` 31건) 합쳐서 판단한다.

**고유명사 후보 — 심볼 테이블 전수 수집**

모든 `.scn` 의 심볼 테이블 문자열을 모아 리소스명을 걸러내면 인명·지명·용어 후보가 남는다.

```python
from collections import Counter
cand = Counter()
for name, off, size in entries:
    if not name.endswith('.scn'): continue
    for s in parse_syms(A.getfile(data, tbl, off, size)):
        if s and not RESOURCE_RE.match(s):
            cand[s] += 1
# 빈도순으로 훑으면 화자명·지명·용어가 위에 모인다
```

**세계관 용어 — 본문 빈출 한자어**

대사·나레이션 텍스트에서 `『…』` `「…」` 로 감싼 어구, 후리가나 표기
(`常闇【とこやみ】の間` 같은 `【】` 패턴)를 뽑으면 설정 용어가 나온다.
후리가나는 읽기가 그대로 적혀 있어 음역 확정에 바로 쓴다.

**장면 구성 — 씬 선택 화면**

씬 선택 스크립트(`scene.scn` 류)의 텍스트는 각 장면 제목이라, 이야기 구조와
등장인물 조합을 한눈에 준다. 스태프롤 스크립트에는 공식 인명·성우가 있다.

### 11-3. 산출물 3종

| 문서 | 내용 |
|---|---|
| **고유명사 표** | 원문 / 읽기 / 한국어 표기 / 분류 / 근거. 확정 전은 `※` |
| **인물 문서** | 프로필, 성격, 관계, **말투 특징**, 성우 |
| **네임플레이트 표** | 게임에 등장하는 모든 화자명의 원문→한국어 대응 |

네임플레이트 표는 화자 태깅 결과에서 자동 생성한다. 태깅에 잡힌 이름은 화면에
그대로 뜨므로 전부 채운다.

### 11-4. 말투 표 (Relationship Register)

인물별이 아니라 "화자 → 상대" 조합별로 정한다. 같은 인물도 상대에 따라 말투가 바뀐다.

```
| 화자 → 상대      | 말투                              |
| A → B (형 같은)  | 반말, 툴툴대는 동생 톤            |
| B → A            | 반말, 놀리는 형 톤                |
| C → A            | 격식체 (과묵·정중, 존댓말은 아님) |
| A → 어른         | 편한 반말 / D → 어른: 존댓말      |
```

화자 태깅이 되어 있으면 번역 덤프에 화자를 실을 수 있고(13절), 이 표를 옆에 두고
기계적으로 적용하면 된다.

### 11-5. 검토 후 시작

사전이 서면 대표 구간(오프닝 등)을 먼저 번역해 검수에 넘긴다.
표기·말투에 대한 피드백을 반영해 사전을 고친 뒤 본편으로 확장한다.
전체를 번역한 뒤 표기를 바꾸면 되돌릴 양이 커진다.

---

## 12. 번역 실무 기법

### 12-1. 중복 텍스트 일괄 처리

같은 대사가 여러 번 나온다. 고유 본문만 번역하면 작업량이 줄고 같은 문장이 같게 번역된다.

```python
key = split_tail(jp)[0]      # 꼬리 제어코드 뗀 본문으로 묶는다
```

神無ノ鳥 실측: 46,182조각 → 고유 본문 38,829개 (15.9% 절감).
`……` 류 짧은 대사와 화자명이 특히 많이 겹친다 (최다 416회).

### 12-2. `sc_` 회상판 파일

`sc_<원본이름>.scn` 은 회상/재생용 사본이다. 神無ノ鳥 는 29개 중 28개가 대응 원본을 갖고,
11개는 텍스트가 완전히 동일하다. 나머지도 상당 부분 겹친다.

→ 오프셋이 다르므로 원문 텍스트로 매칭해 번역을 옮긴다 (패치 아카이브 주입과 같은 방식).

### 12-3. 화면 제약

`common.csv` 의 `textwindow` 선언이 실제 표시 영역이다.

```
textwindow2,textwin,0,300,640,170,50,145,20,20,0,20   ; 본문: 640x170, 좌50/우145 여백
textwindow,namewin,155,320,125,20,...                 ; 네임플레이트: 125x20
textwindow,select,20,160,600,200,...                  ; 선택지: 600x200
```

- 화자명은 네임플레이트 창 125px 안에 들어가야 한다. 원문도 최장 5자
  (`レンジャク` `セキレイ`)로 맞춰져 있다.
- 줄바꿈은 자동이 아니다. 원문에 줄바꿈 커맨드(`\n`)가 들어 있다.
  번역이 길어져 넘치면 같은 커맨드를 원하는 위치에 추가한다 (커맨드이므로 전각화하지 않는다).
- 길이 감각(神無ノ鳥): 평균 23자, 중앙값 23자, 최대 174자. 40자 초과가 5.9%.
  한국어는 같은 내용이 보통 더 길어지므로 긴 대사부터 넘침을 확인한다.

### 12-4. 후리가나 `【】`

`『常闇【とこやみ】の間』` 처럼 읽기가 본문에 박혀 있다 (神無ノ鳥 45조각).

- 음역을 확정할 때 그대로 근거로 쓴다 (11-2 참조).
- **번역문에는 `【읽기】`를 남기지 않는 것이 기본이다.** 후리가나는 일본어 독자용
  읽기 보조라, 한국 독자에겐 `『상암【도코야미】의 방』`처럼 무의미한 일본어 읽기가
  된다. GLOSSARY에서 가나 읽기/한자 독음 중 하나로 **확정한 표기만** 쓴다
  (예: `常闇`→**상암**, 병기 없이). 유지 여부는 타이틀 정책(GLOSSARY)을 따른다.
  `【】` 자체는 표시 문자라 CP949 로 인코딩되며 엔진 파싱엔 영향이 없다.

### 12-5. 여는 괄호는 번역 대상이 아니다

대사 프레임에서 여는 `「`/`（` 는 텍스트 런 앞의 별개 표시 문자다 (3절 프레임 참조).
번역 런에는 닫는 `」`/`）` 만 포함된다.

→ 번역문 앞에 `「` 를 직접 붙이면 괄호가 두 개 나온다. 닫는 괄호만 그대로 둔다.
→ 미번역 조각을 SJIS 로 남기면 여는 괄호만 깨진다 (13절 증상표 참조).

### 12-6. 전각 정규화의 부작용

ASCII 를 전각으로 바꾸면 숫자·영문이 `５분` `ＯＫ` 처럼 폭이 넓어 어색해진다.

- 숫자는 가능하면 **한글로 풀어쓴다** (`5분만 더` → `조금만 더`).
- 꼭 필요한 영문·숫자는 **폰트 쪽에서 비례 글리프로 매핑**하면 자연스러워진다 (7절).

### 12-7. 파일명 규칙으로 순서 잡기

시나리오 파일명이 **날짜 기반**인 경우가 많다 (`0506_01` = 5월 6일 첫 장면,
분기는 `a`/`b`/`c` 접미사). 파일명 순으로 번역하면 **이야기 순서대로** 진행되어
문맥·호칭 변화를 놓치지 않는다. 무작위 순서로 번역하면 관계 변화를 반영하기 어렵다.

---

## 13. 작업 순서

**정본은 [docs/BOOTSTRAP.md](docs/BOOTSTRAP.md)** — 5개 프로세스(`init` → `unpack` →
`extract` → [번역] → `build` → `release`)와 각 단계가 채우는 config 가 거기 있다.
여기서는 이 절 이후의 엔진 작업이 그 순서에서 어디에 놓이는지만 짚는다.

```
extract  -> 텍스트·화자·exe 문자열·이미지 목록 (2·4·11절)
[번역]   -> 사전 구축(11절) 먼저 -> 대표 구간 -> 검수 -> 본편 확장 (12절)
build    -> exe(6~10절) · 주입+재매핑(2·3절) · 제목(1절) · 이미지(14절) · 무비
             SCALE>1 이면 해상도 확대(15절)가 각 단계에 붙는다
폰트     -> ujyu font (7·16절). 빌드와 별개로 시스템에 설치해야 화면에 나온다
```

사전은 번역 내내 갱신한다. 새 고유명사·새 화자가 나오면 그때그때 표에 넣는다.

### 원본에서 한 번에 빌드

무패치 원본을 별도 폴더(`ORIG_DIR`)에 두고, 빌드는 **매번 거기서 다시 생성**해
배포 폴더(`GAME_DIR`)에 쓴다. 그래야 몇 번을 돌려도 결과가 같다. `ujyu build`
가 6~10 단계를 한 번에 돈다:

```
ujyu build          # exe + scenario + 패치아카이브 + 제목 + 이미지
```

순서 주의: **창 제목(common.csv)은 텍스트 주입 뒤에** 와야 한다. 텍스트 주입은
아카이브를 원본에서 다시 패킹하므로, 앞서 넣은 제목이 그때 원본값으로 되돌아간다.
`ujyu build` 는 이 순서를 강제한다. exe 는 `ujyu exe` 가 원본 exe 하나에서
UI(10절)→엔진(6·8절)→코드케이브(9절) 순으로 만들어 낸다.

### 작업 파일 포맷

한 조각 = `{file, off, bytelen, jp, kr, speaker}`. `off`/`bytelen` 은 원본 기준이다.

주입할 때 같은 파일 안에서는 오프셋 내림차순으로 치환한다. 그래야 아직 처리하지
않은 앞쪽 조각의 원본 오프셋이 유효하게 남는다. (텍스트는 널 종단이고 절대 오프셋
테이블이 없어 길이가 바뀌어도 안전하다.)

### 번역 덤프는 화자를 같이 실을 것

```
<id>\t<화자>\t<본문>
```

인물별 어조(반말/격식체/존댓말)와 상대별 관계를 표로 만들어 두고 번역 내내 참조한다.
꼬리 제어코드는 덤프에서 빼고 반영할 때 자동 복원한다.

### 검증 루프 (주입 전)

1. **인코딩 검증** — 전 조각을 CP949 로 인코딩해보고 `0x80` 미만 바이트가 나오는지 확인.
2. **제어코드 대조** — 번역문 꼬리가 원문 꼬리와 일치하는지 확인. 대사 끝 `%` 가
   빠지거나 전각 `％` 로 바뀌면 스크립트가 깨진다.
3. **재로드 확인** — 리팩한 아카이브를 다시 언팩해 해당 오프셋이 의도한 문자열로
   디코드되는지 본다.

### 증상 → 원인

| 증상 | 원인 |
|---|---|
| 한글이 반각 가나(`ｱｳﾀｻ…`)로 나옴 | 리드비트맵 미패치 — CP949 리드가 1바이트로 쪼개짐 |
| 한글이 전부 `?` | 리드비트맵 미패치 + 한글 폰트(반각가나 글리프 없음) |
| 여는 `「` 만 깨짐 | 미번역 조각을 SJIS 로 둠 → 닫는 `」`(CP949)와 비대칭. 미번역분도 CP949 로 변환한다 |
| 대사 뒤가 밀리거나 멈춤 | 꼬리 커맨드(`%`) 유실 또는 전각화 |
| 커맨드가 화면에 `％` `＼ｎ` 로 보임 | 커맨드를 분리하지 않고 런 전체를 전각 정규화함 (2절) |
| 번역이 일부 파일만 반영 안 됨 | 패치 아카이브(`ax2~`)가 덮어씀 |
| 글자 아래가 잘림 | 폰트 아웃라인 세로 시프트 안 함 (메트릭 수정은 무효) |
| 낱말 간격이 과도하게 넓음 | 전각공백 advance 미조정 |
| 글꼴 목록이 비어 있음 | 열거 charset 미패치 / 이름 필터 접두어 불일치 |
| 폰트 설치 거부 | CFF→glyf 변환 후 `sfntVersion` 이 `OTTO` 로 남음 |

### 디버깅 요령

주소가 안 잡히면 로그 브레이크포인트로 값만 기록할 수 있다 (멈추지 않는다):

```
bp <주소>
SetBreakpointCondition <주소>, 0
SetBreakpointLog <주소>, "FONT face={s:eax+0x1c} charset={x:byte(eax+0x17)}"
```

- 폰트 이름 확인: `CreateFontIndirectA` 직전, LOGFONT 포인터의 `+0x1C`(face) / `+0x17`(charset).
- 문자 분할 확인: 글리프 렌더 함수 진입 시 문자 코드 레지스터를 찍어
  2바이트(`0xB1B3`)인지 1바이트(`0xB1`)인지 본다.
- 텍스트 버퍼 추적: 덤프에서 해당 바이트에 하드웨어 접근 BP → 콜스택으로 역추적.
- 파일 접근 확인: `CreateFileA` 에 로그 BP, `{s:[esp+4]}` 로 경로 기록.

---

## 14. 이미지 텍스트 지역화

UI 일부는 글자가 **PNG 에 구워져** 있다 (버튼·화면 제목·엔딩 카드 등). 스크립트만
번역하면 화면에 원문이 남는다. 이미지 안 글자를 지우고 번역문을 다시 그려 넣는다.

### 폴더 컨벤션

같은 파일명으로 단계별 폴더를 둔다. 파일명은 **아카이브 엔트리명과 동일**해야
리팩 때 매칭된다.

```
IMAGE_ORIGINAL_DIR   원문 이미지 (글자 있음)       ← 측정 기준
IMAGE_TEXTLESS_DIR   글자 지운 무문자 베이스       ← 렌더 입력
texted-{폰트}         폰트별 렌더 출력             ← 여러 개 만들어 비교
CG_TRANS_DIR         그중 빌드에 쓸 폴더 (config)  ← build_patch 가 이걸로 cg 아카이브 리팩
```

여러 폰트로 렌더해 비교하고, **채택한 폴더만 `config.CG_TRANS_DIR` 에 지정**한다.
그러면 `ujyu build` 의 cg 단계가 항상 그 폴더로 리팩한다 (13절). 폰트를 바꾸려면
`CG_TRANS_DIR` 만 다른 폴더로 바꾸면 된다.

### 14-1. 텍스트 이미지 식별·수집

이미지 아카이브를 언팩해 **글자가 있는 것만** 골라 `IMAGE_ORIGINAL_DIR`(original) 로 모은다.
on/off/select 처럼 색만 다른 세트는 한 벌로 묶어 다룬다.

- 자동 후보: 알파 채널이나 색 분포로 "글자 같은 획"이 있는 이미지를 1차 선별.
- 확정: 사람이 눈으로 본다. 아이콘·숫자·일러스트·로고(붓글씨 등)는 대개 제외.
- 제외 목록도 명시적으로 남긴다 (나중에 "빠뜨린 것"과 구분하기 위해).

### 14-2. 텍스트 제거 + 배경 복원

글자만 지우고 **그 자리 배경을 자연스럽게 채워** `IMAGE_TEXTLESS_DIR`(textless) 로 저장한다.
배경 성격에 따라 방법을 고른다:

- **단색·단순 버튼**: 배경색으로 덮고 다시 그리는 편이 깨끗하다.
- **그림 위 글자(그림자·그라데이션)**: 인페인팅이 필요하다.
  - 외부 AI 활용 — 이미지 텍스트 제거/인페인팅 도구(예: `translate-image` 스킬)에
    글자 영역을 넘겨 배경을 생성시킨다. 복잡한 배경에 가장 자연스럽다.
  - 로컬 인페인팅 — `cv2.inpaint`(TELEA/NS) 로 글자 마스크 영역만 복원. 마스크는
    글자색 임계값 + 팽창(dilation)으로 만든다. 배경이 단순하면 충분하다.
- 글자 마스크는 **글자·외곽선·그림자 픽셀만** 포함하고 나머지는 원본 그대로 둔다.

무문자 베이스가 서면 원본과 나란히 두고 **지워진 자리에 잔상이 없는지** 확인한다.

### 14-3. 배치 명세 (spec markdown)

무엇을 어디에 어떻게 그릴지 표로 정리한다. 타이틀 리포의 명세 문서(예: 神無ノ鳥 는
`translation/IMAGES.md`)에 표로 적고, 이 표가 렌더 스크립트(14-4)의 배치 데이터 원천이
된다 (**열은 타이틀에 맞게 바꾼다**). 한 행에 담는 것:

| 항목 | 뜻 |
|---|---|
| 파일 | 아카이브 엔트리명(=파일명) |
| 원문 → 번역 | 지울 원문과 넣을 번역문 |
| 원본 텍스트 bbox | 원본에서 **실제로 보이는 글자·외곽선·그림자의 픽셀 박스** (여유 영역 아님) |
| 크기 | 외곽선 포함 실제 렌더 높이 목표(px). 글꼴마다 이 높이에 맞는 명목 크기를 따로 잡음 |
| 색 / weight / 테두리 / 자간 / 정렬 | 원본을 재현하는 렌더 파라미터 |
| 상태 | 스프라이트면 `off`/`on`/`select` (색만 다른 세트) |

원칙:
- bbox 는 레이아웃 여백이 아니라 **원본 글자의 실제 잉크 범위**를 잰 값이다.
- 가로 overflow 는 허용하되, 글자가 **이미지 캔버스**를 벗어나면 전체가 들어올 때까지
  글꼴 크기만 줄인다. 줄바꿈·자르기는 하지 않는다.
- 크기는 명목 pt 가 아니라 **외곽선 포함 실제 높이 목표**로 적는다 — 글꼴마다 실제
  높이가 다르므로 렌더러가 목표 높이에 맞는 크기를 자동으로 고른다.

### 14-4. 폰트별 렌더

타이틀별 PIL 렌더 스크립트를 작성한다 (예: 神無ノ鳥 는 `ujyu image`).
배치 명세(14-3)의 각 항목을 그리는 **재사용 프리미티브**를 갖추고, 화면 그룹별 함수
(타이틀 카드·CG 갤러리·설정 등)로 배치를 코드에 담는다. 프리미티브가 처리할 것:

- **목표 높이 → 글꼴 크기 자동 선택** (글꼴마다 실제 글자 높이가 다르므로).
- **목표 폭 → 자간 자동 조정** (넘치면 폭 기준으로).
- **캔버스 이탈 시 크기만 축소** (14-3 원칙).
- **off/on/select 상태색**, **저장 시 원본 모드(RGB/RGBA) 보존**.
- 원본(`IMAGE_ORIGINAL_DIR`)의 알파/색으로 실제 bbox 를 재고, 무문자 베이스
  (`IMAGE_TEXTLESS_DIR`) 위에 합성한다.

입력·폰트·출력은 config 로 받는다: `IMAGE_ORIGINAL_DIR`·`IMAGE_TEXTLESS_DIR`·
`IMAGE_FONT_DIR`, 출력은 `IMAGE_TEXTED_PREFIX + {폰트변형}`(예: `texted-Minguk`).
폰트만 바꿔 여러 변형을 만들어 비교하고, 채택 폴더를 `config.CG_TRANS_DIR` 에 지정하면
`ujyu build` cg 단계가 그 폴더로 cg 아카이브를 리팩한다(13절).
붓글씨 로고처럼 재현이 어려운 것은 원본 유지가 낫다. 저작권 표기는 유지한다.

### 14-5. 크기 불변 제약

버튼 좌표·클릭 영역이 스크립트에 박혀 있을 수 있다. **출력 이미지 해상도는 원본과
같아야 한다.** 리팩 전에 교체 이미지 전부의 크기를 원본과 대조해 확인한다
(`ujyu build` 의 cg 단계가 엔트리명으로 매칭하므로, 크기만 맞으면 안전하다).

---

## 15. 해상도 확대 (N× 스케일)

엔진이 해상도를 그리는 구조는 [docs/formats/RESOLUTION.md](docs/formats/RESOLUTION.md)
(단일 출처). 이 절은 절차와 찾는 법. 배율은 타이틀 `config.py` 의 `SCALE`(정수, 1=원본).

게임을 원래 해상도의 정수배(예 3×: 640×480 → 1920×1440)로 키운다. 창·백버퍼를
키우고, 데이터 주도 레이아웃(`common.csv`)과 이미지를 N× 로 바꾼다. 검증: 神無ノ鳥 3×.

### 15-1. 렌더 구조

순수 GDI 소프트웨어 컴포지터다 (DirectX·StretchBlt 없음). `CreateDIBSection` 백버퍼에
`BitBlt` 로 1:1 합성한다. 따라서:

- 업스케일 경로가 없다 — 이미지는 **네이티브 크기 그대로** 그려진다.
- 백버퍼는 창 크기를 따라간다.
- 레이아웃은 데이터 주도(`common.csv`)라 좌표만 ×N 하면 텍스트·UI 가 따라 커진다.
- 이미지는 **N× 이미지로 교체**해야 커진다.

### 15-2. exe 화면 크기 루트

창·백버퍼·풀스크린 사각형을 만드는 곳의 상수를 N× 로 바꾼다.

**찾는 법**: 원래 폭(640=`0x280`)·높이(480=`0x1E0`)를 만드는 `mov reg,imm` / `push imm`
을 찾는다. `CreateWindowExA` 클라이언트 크기, 백버퍼 DIB 생성, 화면 크기 초기화(설정
파서) 등 몇 곳이다. `LARGEADDRESSAWARE` 플래그는 이 크래시(15-4)와 무관하다 — 원인은
서피스 치수이지 총 메모리가 아니다.

### 15-3. common.csv 스케일

레이어·텍스트창·글자 크기·선택지 파라미터의 좌표/치수를 ×N.

- `layer,*` : 필드 [2:6] (x,y,w,h) ×N.
- `textwindow*` : 위치·크기·여백 ×N.
- `int,text_size` / `text_line_height` / `select_*` ×N.
- 볼륨이 여러 개면(`scenario.ax2~`) **각 볼륨의 `common.csv` 를 다 고친다**. 번호 큰
  볼륨이 덮어쓰므로(1절), 원본 볼륨만 고치면 반영되지 않는다. `common.csv` 는 시작 시
  로드되므로 값을 바꾸면 재시작해야 한다.

**CP949 값 보존**: `common.csv` 에 CP949 창 제목(`string,title`)이 있으면, 파일을 cp932
로 decode→re-encode 하는 스케일링이 **CP949 트레일 바이트를 깨뜨린다** (cp932 에 없는
바이트가 `?`=`0x3F` 로 치환). 실측: "새"(CP949 `BB F5`) → `BB 3F` ("칸나기의 새"→"칸나기의 ?").
숫자만 바꾸면 되므로 **바이트 단위 치환**으로 처리하고 파일 전체를 재인코딩하지 않는다.

### 15-4. 고정 서피스 크래시

N× 이미지가 **작은 서피스에 들어가면 크래시**한다. 배경·캐릭터처럼 서피스가 이미지
크기에 자동으로 맞춰지는 것은 N× 가 그냥 되지만, 로고·타이틀버튼·메뉴처럼 **서피스
크기가 스크립트/설정에 박혀 있는** 것은 원래 크기로 서피스가 생기고 N× 이미지가 안 맞아
clamp→크래시한다.

**증상**: N× 이미지 로드 중 "abnormal program termination"(Visual C++ runtime), 또는
vtable 파괴 후 가비지 주소 점프(예 `0x80808080`).

**진단 — clamp 지점**: PNG 로더가 서피스 크기와 이미지 크기를 비교해 이미지를 서피스로
깎는다:

```asm
mov eax,[surface_w]
mov ecx,[png_w]
cmp eax,ecx
jge no_clamp        ; surface_w >= png_w 면 통과
mov ecx,eax         ; png_w = surface_w  (CLAMP → 이후 픽셀복사에서 죽음)
```

이 `cmp`+`jge` 에 로그 BP 를 걸어 `(surface_w, png_w)` 를 찍는다 (조건을 `0` 으로 두면
멈추지 않고 로그만 쌓인다, 13절). `surface_w < png_w` 인 로드가 범인이고, `png_w / N` =
원래 폭이라 어느 이미지인지 특정된다.

**해결 1 — 서피스 치수를 ×N**: 서피스 크기의 출처(15-5 그리기 명령, 또는 `common.csv`
레이어)를 찾아 ×N 하면 서피스가 N× 이미지에 맞게 커진다.

> clamp 자체를 없애는(`jge`→`jmp`) 패치는 하지 않는다 — 서피스 리사이즈 가상함수가
> 안전하게 재할당하지 않아 힙이 깨진다 (실측: 큰 이미지 로드 중 vtable 파괴).

**해결 2 — 그 이미지는 1× 유지 (임시/보류용)**: 그 화면의 그리기 명령이 너무 복잡해
치수 스케일이 위험하면(좌표·인덱스·플로우가 얽힘, 15-7) **당장은** 이미지를 원본 크기로
두어 서피스와 맞춘다.

> **⚠️ 神無ノ鳥 실측 정정**: 처음엔 로고·타이틀·UI·얼굴의 서피스 치수가 "exe 하드코딩이라
> 못 찾음"으로 판단해 1× 로 뒀으나(그래서 클램프 크래시를 겪는다고 오해), **실제로는
> 그 치수가 전부 `.scn` 명령층(15-5)에 있었다.** exe 가 전역으로 고정하는 건 **화면 크기
> (640×480) 루트뿐**이고(15-2: 神無ノ鳥 실측 = 4곳 폭0x0280→0x0780·높이0x01e0→0x05a0 +
> LARGEADDRESSAWARE, 총 9바이트), **개별 아이템의 배치·서피스 치수는 전부 `.scn` 안**에 있어
> 뜯어서 ×N 하면 된다. 따라서 "고정 서피스 클램프 크래시" 자체가 (전역 화면 외엔) 없고,
> 1× 유지는 "못 찾아서"가 아니라 "그리기가 복잡해 나중에 하려고"인 경우에만 쓴다.

### 15-5. VNEG 그리기 명령 (서피스 치수의 출처)

`.scn` 의 서피스 치수는 대개 명령층에 박혀 있다. 3절 심볼 테이블의 일반
문법은 다음과 같다:

```
정의 = <opcode:u8> <slots:u16 BE> <argcount:u8> + argcount개 인자
인자 = 01 <int32 BE>     (정수)
     | 00 <cstr\0>       (문자열)
```

| op | 타입 (exe 타입 레지스트리 실측) | 인자 |
|---|---|---|
| `0x02` | int | 정수 리터럴 |
| `0x03` | string | 문자열 리터럴 (리소스명·화자·대사) |
| `0x07` | layer | 앞 4개 = x,y,w,h (표시/서피스) |
| `0x08` | textwindow | 앞 4개 = x,y,w,h (UI 요소 영역) |
| `0x0c` | button | 앞 4개 = x,y,w,h (버튼 영역) |
| `0x16` | scroll | 앞 4개 = x,y,w,h, arg4=bool, arg5=int |

(구명칭 DRAW/RECT/SCREEN 은 오판이다. exe 레지스트리의 전체 23타입
`object`–`scroll`은 `docs/formats/VNEG.md` §5 참조.)
`0x07`/`0x08`/`0x0c`/`0x16`의 **앞 4개 인자가 치수**다. 이걸 ×N 하면 서피스가 커진다. 예: 로고
`mlogo.scn` 의 `DRAW 320,240,280,240,...` 을 ×3 하면 280폭 서피스가 840 이 되어 3× 로고가
맞는다. 타이틀버튼 `title.scn` 의 `INT 153`(버튼 폭)도 서피스 폭이므로 ×3.

> **⚠️ 이미지를 N× 하면, 그 이미지를 표시하는 모든 씬의 `.scn` 치수도 N× 해야 한다** (자주
> 빠뜨림). cg 아카이브에서 이미지만 키우고 `.scn` 치수를 안 고치면 N× 이미지가 1× 서피스에
> 안 맞는다. 로고·타이틀·CG뿐 아니라 **명시적 치수로 이미지를 그리는 모든 씬**이 대상.
> (배경·캐릭터는 `common.csv` 월드 레이어로 그려져 `.scn` 치수가 없으니 예외 — 레이어만 ×N.)
>
> - **인코딩(실측)**: 치수는 명령 영역에 **2바이트 BE 로도** 박힌다(`01 <int32>` 만이 아님).
>   `mlogo.scn` 은 이미지 4개(`miris_logo1~4`)마다 `x,y,w,h=320,240,280,240`
>   (`01 40 00 f0 01 18 00 f0`)이 2바이트씩 있고, ×3 하면 `03 c0 02 d0 03 48 02 d0`
>   (960,720,840,720). 오프셋은 심볼/명령 영역(flow_start 이전).
> - **빠진 씬 찾는 법**: 각 씬을 **원본(pre-N×) 아카이브와 바이트 diff**. 이미지 표시 치수가
>   원본과 동일(안 커짐)한데 그 이미지는 cg 에서 N× 면 = 빠진 씬. diff 위치 값이 640/480 계열
>   (320·240·280·153 등)이면 ×N 대상. 인덱스·색상·타이밍(15-5 아래 주의)은 제외.

> 명령층에 안 맞는 바이트(3번째 바이트가 `01` 이 아닌 `01 10 XX`·`76`·`04 7F 00 74` 등)는
> 대사 플로우 마이크로바이트코드다. 좌표 스케일 시 인덱스·색상·타이밍 정수는 건드리지
> 않는다. 용도가 검증되지 않은 정수까지 일괄 확대하지 말고, 객체 타입·메서드 시그니처와
> 심볼 타입이 확인된 좌표만 바꾼다. 神無ノ鳥 `config.scn`은 다원소 심볼 슬롯과
> `button.m02` 시그니처를 규명해 자동 스케일 대상으로 전환했다(VNEG §6-5).

명령층 디스어셈블은 `ujyu`(= `ujyu.formats.vneg`) 참조 (심볼 테이블은 전체 23타입으로
식별하되, FLOW 명령은 검증된 5개 opcode 만 인식하고 나머지는 플로우 hex 로 표기 — 우연 정렬
오검출 방지).

### 15-6. 대사 UI 를 구석에

큰 아트 + 작은 텍스트박스 배치. 배경·캐릭터는 N×, 대사창·컨트롤·얼굴은 **원본 크기로
되돌리고 위치만 이동**한다.

- 대사 UI 이미지(대사창 배경·컨트롤 아이콘·프레임·커서·얼굴)는 cg 아카이브에서 **1× 유지**.
- `common.csv` 텍스트 UI(`textwin`·`namewin`·`select`·`text_size`)는 원본값으로 되돌리고
  **화면 확대분만큼 오프셋** 이동한다. 원래 UI 를 델타 `(W_new−W_orig, H_new−H_orig)`
  만큼 옮기면 상대 배치를 유지한 채 우하단 구석으로 간다.
- 얼굴 레이어(`layer,face`)도 원본 크기 + 같은 델타. 원래 텍스트창 좌측 초상 슬롯
  (문장紋章 자리)에 들어간다.

### 15-7. 메뉴

메뉴(설정·세이브·로드·뮤직·장면·CG갤러리)는 화면마다 판단한다.

> 콘텐츠 이미지를 AI 업스케일러(Topaz 등)로 키우는 절차는
> [docs/formats/RESOLUTION.md §5-1](docs/formats/RESOLUTION.md) — `ujyu scale cg-export`
> 로 대상만 뽑아 돌리고 `ujyu scale cg --from-dir` 로 되돌려 넣는다.

- **1× 유지**(안전): 메뉴 UI 이미지를 원본 크기로 두면 원래 좌표대로 떠서 크래시하지
  않는다. 좌표가 아직 규명되지 않은 화면은 화면별 이미지 프리픽스로 골라 1× 로 둔다
  (神無ノ鳥: `mu_` `sc_` `cgtop_`).
- **풀스크린 N×**: 스크립트 치수를 ×N (15-5). 좌표가 명확한 화면만 안정적이다.
  神無ノ鳥는 `config_`(설정)와 `sa_`(세이브/로드)의 씬 좌표·이미지를 함께 ×N 하는
  경로가 실기 검증됐다.
- **공유 이미지 주의**: 한 이미지가 여러 화면에 쓰이면(예 `cgtop_back` 이 갤러리+뮤직)
  아카이브 안에서 크기가 하나뿐이라, 한쪽을 N× 하면 다른 쪽이 깨진다. CG 갤러리는
  게임플레이의 N× CG(`cg_background_*`·`event*`)를 그대로 표시하므로 표시부 서피스를
  스케일하지 않으면 크래시한다.

---

## 16. 텍스트 폰트 빌드

게임은 `CreateFontIndirectA(FACE)` 로 시스템 폰트를 찾는다. 한글 폰트를 게임에 맞게
가공해 설치한다. 빌더 `ujyu font` 는 **폰트 스펙 파일**(타이틀 리포
`fonts/<face>.py`)을 받아 빌드한다 — 스펙은 config.py 와 분리한다(GUIDE §1).

### 16-1. 스펙 파일
`fonts/<face>.py` 에 선언한다:
- `SOURCE` — 소스 otf/ttf(한글 폰트) 경로, `FACE` — exe 가 찾는 이름.
- `TARGET_BOTTOM_EM`·`SAMPLE` — 세로 시프트(렌더가 `GGO_GRAY8` 라 디센더가 잘림, 한글
  최저점을 목표 위치로 올림).
- (가변폭용) `SPACE`·`OPEN`+`OPEN_RSB`·`CLOSE`·`FIXED`·`CMAP_ALIAS`.

### 16-2. 렌더링 폭 모드 (`config.FONT_WIDTH_MODE`)
- **`"fullwidth"`** — 모든 글자 전각 고정폭. 괄호·기호 조정을 생략하고 문장부호도 전각
  그대로 둔다(고정폭이라 간격이 자연).
- **`"proportional"`** — 가변폭. 스펙(`SPACE/OPEN/CLOSE/FIXED`)대로 괄호·기호 advance 와
  획 위치를 손보고, `inject_text` 가 마침표·쉼표(`。/、`) 뒤에 공백을 넣는다.
- 세로 시프트·전각 ASCII 재매핑·`CMAP_ALIAS`(글리프 없는 문자 별칭, 예 `～→~`)는
  **모드와 무관하게** 항상 적용한다(글자가 화면에 나오게 하는 것이라).

### 16-3. 괄호·기호 폭 잡는 법 (가변폭)
낫표·소괄호는 획이 작고(「」191·『』277·()257) 전각 셀 한쪽에 몰려 있다. 그래서 반각화는
advance 만 줄이지 말고 획 위치를 함께 손본다:
- **여는(획 오른쪽) 「『(** — advance 를 목표로, 획 오른끝을 `OPEN_RSB` 여백으로 정렬
  → 왼쪽 여백(lsb)이 커져 문두에서 자동 들여쓰기.
- **닫는(획 왼쪽) 」』** — advance = 획 오른끝 + 트인쪽 여백.
- **두꺼운 괄호 【】** — 고정 advance(획이 넘치면 왼쪽으로 밀어 넣음).
- 반각 기준은 폰트 고유값(전각공백 = 한글의 절반)을 쓴다. 소괄호 원본 폭 같은 우연값을
  기준 삼지 말 것.

### 16-4. 빌드·설치
```bash
ujyu font [out.ttf] [--mode fullwidth|proportional]   # 기본 mode = config.FONT_WIDTH_MODE
```
설치는 시스템 폰트 폴더 복사 + `AddFontResource` + `WM_FONTCHANGE` 브로드캐스트. **같은
face 이름이면 GDI 가 옛 글리프 테이블을 캐시**하므로, 파일명을 바꿔 설치하면 캐시를
우회한다(같은 이름·같은 파일명은 갱신이 안 먹는다).

### 16-5. 렌더 제약 (실측)
- 본문 경로는 표시 바이트가 `0x80` 이상이어야 한다(§2). ASCII 는 커맨드.
- 반각 가나 글리프가 없는 한글 폰트에서 미번역분을 SJIS 로 두면 깨진다(§13 증상표).
- 소스에 없는 전각 문자(예 전각 물결 `～` U+FF5E)는 `CMAP_ALIAS` 로 반각 글리프에 연결한다.

---

## 부록 A. 神無ノ鳥 (2002) 실측값

파일오프셋 = VA − `0x400000`. 원본 `kannagi.exe` 기준.
(ASLR 이전 빌드라 베이스가 고정이다. 다른 타이틀이라면 PE `DllCharacteristics` 의
`0x0040` 비트가 없는지 먼저 확인할 것 — 있으면 오프셋 대신 패턴 스캔이 필요하다.)

| VA | 파일오프셋 | 내용 |
|---|---|---|
| `0x477468` | `0x77468` | 리드바이트 비트맵 (32B) |
| `0x438D50` | — | 문자 추출기 (비트맵 사용) |
| `0x408E00` | — | 리더 (바이트 분류) |
| `0x438244` | `0x38244` | 본문 charset (`C6 44 24 27 80`) |
| `0x40CBD1` | `0xCBD1` | 열거 charset (`C6 44 24 37 80`) |
| `0x4381D0` | — | 폰트 빌더 (LOGFONT 조립) |
| `0x4387B0` | — | GGO 렌더 |
| `0x40C8A0` | — | 글꼴 목록 콜백 |
| `0x40C974`/`0x40C983` | `0xC974`/`0xC983` | 비례·FIXED_PITCH 필터 (NOP 대상) |
| `0x40C94A`/`0x40C95A` | `0xC94A`/`0xC95A` | 이름 필터 (제외→포함 전환) |
| `0x475CD0` | `0x75CD0` | 필터 패턴 문자열 (8B 여유) |
| `0x475690` | `0x75690` | GOTHIC 기본 글꼴 (16B) |
| `0x4756A0` | `0x756A0` | MINCHO 기본 글꼴 (16B) — 본문이 쓰는 쪽 |
| `0x477458` | `0x77458` | case-C 폴백 face (16B) |
| `0x41A9C4`/`0x41EA21`/`0x402EA8` | `0x1A9C4`/`0x1EA21`/`0x2EA8` | SJIS idiom (보조) |
| `0x45F1E1` | — | 원래 OEP |
| `0x46DA40` | `0x6DA40` | 코드 케이브 (.text 패딩 1494B) |
| `0x482000` | — | 케이브용 버퍼 (.data 여유 8195B) |

## 부록 B. 도구

구현은 이 스킬의 [`ujyu/`](ujyu/) 에 들어 있다. 타이틀별 설정은 **타이틀 리포 루트의
`config.py`** 한 곳에 모인다 — 템플릿 [`samples/config.py`](samples/config.py) 를 복사해
채우고, 도구는 타이틀 리포 루트에서 실행(또는 `MIRIS_CONFIG_DIR` 지정)한다.

| 도구 | 역할 |
|---|---|
| `titleconfig.py` | 타이틀 설정 로더 ($MIRIS_CONFIG_DIR → CWD 의 config.py) |
| `samples/config.py` | 타이틀 설정 **템플릿** — 경로·아카이브·폰트명·exe 오프셋 |
| `ujyu build` | **전체 빌드** — 원본에서 exe·텍스트·제목·이미지를 한 번에 (13절) |
| `ujyu exe` | **exe 전체 패치** — UI·엔진·코드케이브를 원본 exe 하나에서 (6·8·9·10절) |
| `ujyu axr` | AXRe 언팩/리팩 |
| `ujyu` | VNEG 디스어셈블 / **텍스트 추출(extract)** / 점프테이블 재매핑(relocate) |
| `ujyu nameplates` | 화자명 대응표 생성 (번역표는 타이틀 종속) |
| `ujyu filter` | 번역 대상 선별 + dump/apply (5절) |
| `ujyu inject` | CP949 인코딩·검증·**전체 아카이브 빌드** (유효본 arc별 주입 + 재매핑, 커맨드 보존) |
| `ujyu migrate` | 구(휴리스틱) strings → v2 번역 이관 + 커버리지 QA (일회성) |
| `ujyu title` | `common.csv` 설정 |
| `ujyu scale` | 해상도 N× 스케일 — common/dims/cg/exe (15절, config 주도) |
| `ujyu exe ui` | Windows UI 리소스/인라인 문자열 (10절) |
| `ujyu font` | 게임용 한글 폰트 빌더 (7절) |
| `ujyu exe fontrestore` | 글꼴 저장/복원 코드 케이브 (9절) |
| `ujyu image` | 타이틀별 이미지 텍스트 렌더 (14절, 배치 코드 내장) |
